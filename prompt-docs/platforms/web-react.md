# Web Platform: React

**P2**
Tags: platform, web, react

- Favor composable components and explicit props contracts.
- Keep state localized unless cross-page coordination is required.
- Use semantic HTML and accessibility attributes by default.
- Avoid over-abstracting until repeated patterns are stable.
